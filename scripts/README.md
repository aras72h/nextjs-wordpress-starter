# Scripts

Deployment and utility scripts for nextjs-wordpress-starter.

| Script                 | Purpose                                               |
| ---------------------- | ----------------------------------------------------- |
| `deploy-production.sh` | Runs on production server via SSH from GitHub Actions |
| `deploy-staging.sh`    | Runs on staging server via SSH from GitHub Actions    |

Both scripts are executed remotely — they are not run locally.
